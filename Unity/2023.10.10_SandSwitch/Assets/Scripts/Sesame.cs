using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sesame : MonoBehaviour
{
    public int sesameCount = 10;
    // Start is called before the first frame update
    void Start()
    {
        sesameCount++;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
